mixin like-button(options = {})
  - 
    const {
      counter = 0,
      isPushed = false,
    } = options;

  - let additionalClass = (isPushed) ? "like-button_pushed" : "";
  button.like-button(class= additionalClass)
    span.like-button__counter !{counter}
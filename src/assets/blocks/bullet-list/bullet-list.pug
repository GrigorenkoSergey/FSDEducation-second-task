mixin bullet-list(options = {})
  -
    const defaultOptions = require('Blocks/bullet-list/default-options.json');
    const { items } = { ...defaultOptions, ...options};

  ul.bullet-list
    each val in items
      li.bullet-list__li 
        span.bullet-list__li-text !{val}
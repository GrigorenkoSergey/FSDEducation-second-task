mixin filter-date({ title = "filter date dropdown" } = {})
  .filter-date
    if (title.length !== 0)
      .filter-date__title
        h3 !{title}
    input.filter-date__input.js-filter-date__input(readonly placeholder= 'ДД ММ - ДД ММ') 
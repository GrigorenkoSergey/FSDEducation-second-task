include ../input/input.pug

mixin masked-text-field( options = {})
  -
    const {
      attr = {},
      format = "date",
    } = options;

  input.masked-text-field.js-masked-text-field(type="text" placeholder= "ДД.ММ.ГГГГ" data-format= format)&attributes(attr)
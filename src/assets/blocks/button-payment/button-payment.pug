mixin button-payment(options = {})
  -
    const {
      text = "Оплатить",
      attr = {},
    } = options;

  button.button-payment(type="submit")&attributes(attr)
    span.button-payment__text #{text}
    .button-payment__arrow.material-icons arrow_forward
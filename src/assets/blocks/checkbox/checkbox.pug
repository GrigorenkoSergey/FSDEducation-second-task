mixin checkbox(description = {})
  - 
    var {
      text = "check it!",
      attr = {},
    } = description;

  div.checkbox
    label.checkbox__label
      input.checkbox__input(type="checkbox")&attributes(attr)
      span.checkbox__span !{text}